html
    head
        meta
        style
    body
        div.test
        div&attributes({a:1,b:2,c:3})
        //- 协变量
        div.
            a
        div(class=["a1","a2"])
        div(style={a:1,b:2,c:3})
        -let a =1
        -let b =2
        div 结果是：#{a+b}
        span=test
        - arr =[2,2,4,5,6]
        -for(var i=0;i<arr.length;i++)
            div=arr[i]
        div 
            div.
                这是个傻逼
            aaaa   
                |aaa

        -let content = "<h1>aaaaaa</h2>"
        div!=content
        //- 对于 条件输出中 switch case有点特殊
        -let param = 1;
        case param
            when 0
                div aaa
            when 1
                div bbb
            default 
                不靠谱
